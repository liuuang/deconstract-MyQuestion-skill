---
name: deconstruct
description: 像 /plan 一样互动式拆解问题。先确认方法，再澄清、拆解、获取信息、决策、否定检查、行动。用法：/deconstruct 我想了解医学新闻
user-invocable: true
disable-model-invocation: false
---

# 解构模式（Deconstruct Mode）

这是一个**固定流程工具**，不是自由聊天。默认采用**交互式解构**，每次只推进一个步骤，必须等用户回答后再进入下一步。

核心心法：**先确认方法，再开始做事。方法对了，可能一下就解决；方法错了，研究越久越糟糕。**

## 全局架构：整个流程 = 一次 OODA 循环

整个解构流程是一个完整的 OODA 循环（Observe 观察 → Orient 判断 → Decide 决策 → Act 执行），外加循环前的"定位"和第 5 步质检闸门。步骤与环节一一对应：

```
定位（循环前）  第 0 步 元检查 —— Cynefin 定问题域（含否定分级）+ Rumsfeld 盘知识状态，选方向
Observe 观察    第 1 步 澄清    —— 收集问题现状
Orient 判断     第 2 步 拆解    —— 拆结构、列假设、出候选方案
Observe 观察    第 3 步 获取信息 —— 带着假设清单收集证据（归属 Observe，流程位置在拆解后）
Decide 决策     第 4 步 决策    —— SWOT → Decision Matrix → SMART
质检闸门        第 5 步 否定检查 —— 反馈环：否定成功→回澄清/具体步骤；否定失败→行动
Act 执行        第 6 步 行动    —— PDCA × OODA 双循环 + Eisenhower 落地
```

关系说明：

- 第 0 步是循环前的定位，不占 OODA 四环；它回答"该不该进循环、从哪一环进"，同时决定否定检查的强度（见"否定分级"）。
- **Observe 出现两次**（第 1、3 步），被 Orient 隔开——OODA 是循环，收集信息本就可能多轮。
- 第 5 步是 Decide → Act 之间的质检闸门，且是**反馈环**：否定成功（挑出缺陷）→ 回澄清包重循环或回对应步骤；否定失败（通过）→ 进入行动。
- 工具按"包"挂载到对应步骤，包内顺序固定；每步先问标准提问，再按需跑包内工具（见"工具触发规则"）。

## 用户控制指令

- 直接回答：可以选 A/B/C，也可以自由输入。
- `继续`：当前步骤用合理默认值推进。
- `跳步`：跳过当前步骤。
- `全部`：不再交互，一次性输出完整解构。
- `停止`：结束本次解构。
- `深入`：对当前步骤递归否定检查（等价于跳到第 5 步对该步骤产出做否定，再回来）。

## 工具触发规则（每步怎么跑）

- 每步 = 1 个标准提问（先问）+ 按需跑包内工具（后跑）。
- 用户回答具体内容 → 用回答驱动工具；用户说"继续" → 用合理默认值跑完工具。
- 工具的中间产出（问题树、SWOT 矩阵、CRAAP 评分、决策矩阵等）必须展示给用户——这些是解构的核心价值，不是隐藏过程。
- 工具不全跑：包内工具按分工顺序执行，遇到不适用场景（如问题不涉及外部环境）可跳过该工具并在输出中说明。

## 每步标准提问

- 第 0 步：你希望我先获取信息（检索/请教）、先实验，还是先停下想清楚？
- 第 1 步：你最终想得到什么？时间/范围要求是什么？
- 第 2 步：你希望我从哪个角度拆？
- 第 3 步：你更倾向静态资源（文献/网页/数据）还是人际资源（问人）？
- 第 4 步：候选方案里你倾向哪个？判断标准是什么？
- 第 5 步：你觉得当前方案里哪个环节最可能不靠谱？
- 第 6 步：你希望行动输出成什么形式？

## 工具包（Tool Kits）

工具不零散调用，按"包"组织。**包 = 工具集合 + 内部分工顺序 + 适用场景 + 名字**，调用时只说包名，不逐个罗列工具。

| 包名 | 挂载 | 组成 | 内部分工 |
| --- | --- | --- | --- |
| 澄清包 | 第 1 步 Observe | 5W1H/5W2H → SMART → SCQA | 收集信息 → 目标改写为具体/可衡量/可实现/相关/有时限 → SCQA 重述验证 |
| 拆解包 | 第 2 步 Orient | Issue Tree → MECE → First Principles → PEST/PESTEL | 树拆 → 查互斥穷尽 → 挖不可再分的假设与事实 → 外部环境补充（产出为待验证假设，第 3 步证据回填修正；已验证部分喂给第 4 步 SWOT 的 O/T） |
| 获取信息包 | 第 3 步 Observe | 信源评估（CRAAP 默认，按领域换 EBM/GRADE/JBI）→ 静态 DLC：PICO；人际 DLC：Stakeholder → Power-Interest Grid | 带着第 2 步的假设清单定向收集（假设驱动检索）→ 任何信源先评估可信度 → 静态按领域标证据等级 → 人际验人、列人、排序 → 证据回填修正第 2 步假设 |
| 决策包 | 第 4 步 Decide | SWOT → Decision Matrix → SMART | 内外部扫描 → 多方案打分选方案 → 行动约定写成具体/可衡量/可实现/相关/有时限 |
| 否定包 | 第 5 步 质检（反馈环） | Socratic Method → 5 Whys → Red Team/Dialectics → Pre-mortem | 连问 3 层 → 挖 5 次根因 → 强制反方 → 预演失败；否定成功→回澄清/具体步骤，失败→前进 |
| 行动包 | 第 6 步 Act | PDCA + Eisenhower Matrix；学习任务额外套 Kolb | PDCA × OODA 双循环排周期 → 按重要/紧急排序 → 学习任务走经验/反思/抽象/实验 |

## 必须执行流程

### 第 0 步：元检查（定位）

并行使用 **Cynefin** 与 **Rumsfeld Matrix** 两个维度，各管一事：

- **Cynefin**：判断问题属于清晰/繁杂/复杂/混乱 → 决定用什么方法（方向三选一），同时决定否定检查的强度（见"否定分级"）：
    - **先获取信息**：清晰/繁杂问题——先检索（书面信源，低成本、自主可控）；检索不足以支撑判断时再请教（活人信源，用于判断/立场/经验）。
    - **先实验**：复杂问题，无现成答案——必须动手试。
    - **先停止**：混乱/未定义问题——退回第 1 步重新澄清。
- **Rumsfeld Matrix**：盘点知识状态（已知已知/已知未知/未知未知）→ 决定信息从哪来：
    - 已知未知 → 先检索补齐（知道自己缺什么，定向找）。
    - 未知未知 → 先请教建立概念（不知道自己不知道什么，检索无从下手，活人信源先给框架）。

分支规则（影响后续流程）：

- 先停止 → 回到第 1 步澄清，问题定义清楚后再重新元检查。
- 先实验 → 跳过第 3、4 步（不获取信息、不决策），直接走第 5 步否定检查 → 第 6 步行动。
- 先获取信息 → 正常走完整流程。

注：Rumsfeld Matrix 全流程仅在第 0 步使用一次（定知识状态）。

### 否定分级（Cynefin 决定否定强度）

Cynefin 判定不仅选方法，还决定否定检查的强度：

| 问题域 | 流程 | 否定强度 |
| --- | --- | --- |
| clear（清晰） | 完整流程 | 仅决策包后一次（完整四件套） |
| complicated（繁杂） | 完整流程 | 每包后完整四件套 + 决策包后最大否定 |
| complex（复杂） | 实验路径（跳过第 3、4 步） | 每个实际经过的包后完整四件套 |
| chaotic（混乱） | 先停止，不进流程 | 不进入流程（澄清转域后按新域规则） |

- "每包后检查" = 该包产出后跑完整否定四件套（Socratic → 5 Whys → Red Team → Pre-mortem）。
- **最大否定仍在决策包后**：唯一决定"能否进入行动"的闸门，任何域都保留。
- 分级不影响第 5 步的反馈语义：否定成功 → 回澄清/回对应步骤；否定失败 → 前进。

### 第 1 步：深度澄清 → 澄清包（Observe）

依次执行 **5W1H/5W2H → SMART → SCQA**。

- **5W1H / 5W2H**：回答 What / Why / Who / Where / When / How / How much。
- **SMART**：把目标改写成具体、可衡量、可实现、相关、有时限。（注意：此处 SMART 澄清目标；第 4 步 SMART 写行动约定，用途不同）
- **SCQA**：用 Situation → Complication → Question → Answer 重述问题。

### 第 2 步：拆解 → 拆解包（Orient）

依次执行 **Issue Tree → MECE → First Principles → PEST/PESTEL**。

- **Issue Tree**：把大问题逐层拆成问题树。
- **MECE**：检查子问题是否相互独立、完全穷尽。
- **First Principles**：列出假设与不可再分的事实。
- **PEST / PESTEL**：涉及外部环境时，从政治、经济、社会、技术、环境、法律拆。**注意：此处产出是"待验证假设清单"，不是分析结论**——靠已有知识枚举"该考虑哪些外部维度"，给第 3 步提供检索目标；第 3 步证据回来后回填修正，已验证部分再喂给第 4 步 SWOT 的机会/威胁象限。

### 第 3 步：获取信息 → 获取信息包（Observe）

= 第 0 步"先获取信息"方向的落地。核心动作是**信源评估**：任何信息源（资料或人）先评估可信度，再决定用不用。

- **假设驱动检索**：第 2 步产出（问题树叶子、First Principles 假设、PESTEL 待验证假设）就是本步的检索目标——先靠已有知识列"可能要查什么"，再带着目标去收集，避免无方向乱搜。证据回来后**回填修正第 2 步的假设**。

- **信源评估（Appraisal）**：默认 **CRAAP Test**（时效/相关/权威/准确/目的），通用五维，资料和活人都能验；按领域换工具：EBM 证据等级（医学）、GRADE（指南）、PRISMA/CONSORT/STROBE（报告质量）、JBI 清单（护理）等，选领域对应的，并标证据等级。
- **静态 DLC**（书/文献/网页/数据）：PICO 结构化检索 → 按领域选评估工具标证据等级。
- **人际 DLC**（专家/同行/有经验者）：CRAAP 验人 → Stakeholder Analysis 列人（谁最了解、谁受影响、谁有立场）→ Power-Interest Grid 四象限排序，决定优先找谁。

### 第 4 步：决策 → 决策包（Decide）

依次执行 **SWOT → Decision Matrix → SMART**。

- **SWOT**：分析优势、劣势、机会、威胁。机会/威胁可吸收第 2 步 PESTEL 经第 3 步验证后的产出（回填修正过的），也可吸收第 3 步收集的证据。
- **Decision Matrix**：多方案打分（评分维度来自 SWOT 的关键因素），选方案。
- **SMART**：把选定的方案写成具体、可衡量、可实现、相关、有时限的行动约定。

### 第 5 步：否定检查 → 否定包（质检闸门，反馈环）

对第 4 步的决策（或指定步骤的产出）依次执行 **Socratic Method → 5 Whys → Red Team/Dialectics → Pre-mortem**。

- **Socratic Method**：对每个关键结论连续追问至少 3 层。
- **5 Whys**：对可疑环节连续问 5 次为什么。
- **Red Team / Dialectics**：强制生成反方观点。两者是"制造反方"的两种机制，可二选一或都用：**Red Team = 外部角色扮演**（模拟一个对手/攻击者专门挑毛病，补外部视角）；**Dialectics = 内部逻辑推演**（正题→反题→合题，从论证内部挖矛盾，补逻辑自洽性）。
- **Pre-mortem**：假设方案已失败，倒推原因。
- **反馈环**（替代原"无限递归"的模糊回退）：否定成功（挑出缺陷）→ 默认回第 1 步澄清包重循环；若缺陷定位明确 → 只回对应步骤修正后回到本步。否定失败（没挑出毛病）→ 进入行动。回退不设深度上限，除非用户要求停止。
- **否定分级**：按第 0 步 Cynefin 判定——clear 仅本步做一次；complicated/complex 每个包产出后都要做完整否定（见"否定分级"），本步的决策包后否定是其中最大的一次。

### 第 6 步：输出行动网络 → 行动包（Act）

执行 **PDCA + Eisenhower Matrix**：PDCA 排 Plan-Do-Check-Act 周期，Eisenhower Matrix 按重要/紧急排序。学习任务额外套 **Kolb Learning Cycle**：经验 → 反思 → 抽象 → 实验设计。

**PDCA × OODA 双循环**：两模型是同一个旋转的圆——OODA 管"想清楚"，PDCA 管"执行好"，共享反馈精神：

- **P ≈ OOD**：决策前思考环（观察→判断→决策），规划阶段把 OOD 全包了。
- **D ≈ A**：执行，唯一严格一一对应的一环。
- **C ≈ 下一圈 Observe**：检查 = 对执行结果的再观察，是下一圈 OODA 的起点，不是 A。
- **A ≈ 下一圈 Orient + Decide**：根据反馈调整 = 再判断再决策，然后进入下一圈 D。

表述：**一次 PDCA 循环 = OODA 决策 → 行动 → 再观察 → 再调整**，两模型旋转嵌套。

## 输出要求

- 每个步骤先给简短说明，再问 1–2 个关键问题。
- 必须等用户回答后再进入下一步。
- 用户没输入"继续"时，不能用默认值擅自推进。
- 用户没输入"全部"时，不能一次性输出完整结果。
- 每个问题必须同时提供选项和开放式输入空间。
- 工具的中间产出（问题树、SWOT、决策矩阵等）要展示，不隐藏。
